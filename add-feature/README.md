# add-feature

Log a feature request to `docs/features.md` with description, priority, and implementation status.

## Usage

```
/add-feature
```

Describe the feature. The skill appends a structured entry to `docs/features.md` (created if missing). It will **not** auto-implement — it only logs.
